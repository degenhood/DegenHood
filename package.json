{
  "name": "degenhood-developer-tooling",
  "private": true,
  "version": "0.1.0",
  "description": "Public SDK, CLI, API specification, and agent workflow for DegenHood.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs && npm test --prefix packages/sdk && npm test --prefix packages/cli",
    "verify:packages": "node scripts/verify-package-release.mjs",
    "smoke:read-only": "node scripts/smoke-developer-lane.mjs"
  }
}
